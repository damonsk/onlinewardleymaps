# This file configures npm for the project
# Forces use of exact versions for stability
save-exact=true
# Skip warnings about peer dependencies
legacy-peer-deps=true
# Ensures types are published properly with package
types=true
# Ensures CI builds use the node_modules directory
prefer-offline=true
# Important for CI to avoid installing dev dependencies in production
only=production
