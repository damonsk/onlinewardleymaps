name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@master
    - uses: Borales/actions-yarn@v2.1.0
    - name: building for produciton
      with:
        cmd: yarn install
        
