name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@master
      
    - name: yarn install
    - uses: Borales/actions-yarn@v2.1.0
    - run: |
        yarn install
